
# Environment variables (centralized in root .env)
# Root .env is tracked and encrypted with git-crypt (see .gitattributes)
# All code uses tools/dotenv.py to load from root .env
.env.local
.env.*.local
.install-*

# Claude Code local settings (machine-specific preferences)
.claude/settings.local.json

# Python cache and compiled files
__pycache__/
*.py[cod]
*$py.class
*.so

# Build, distribution, and packaging
build/
dist/
*.egg-info/
*.egg

# Testing outputs
.pytest_cache/
.coverage*
coverage.xml
.htmlcov/

# Flask instance folder
instance/

# VS Code history
.history/

# Specstory tool
.specstory/

# UV virtual environment
.venv/
env/

# Runtime cache directories (lesson caches, etc.)
.cache/
**/.cache/
cached_content/

# Compose: Let's Encrypt certificates (contains account private key)
compose/acme/

# Compose: Local .env copy (root .env is git-crypt encrypted, this is a copy for docker-compose)
compose/.env

# Compose: Data files - track archives, qdrant_storage, queues
# Machine-specific files and temp files are ignored
compose/data/*
!compose/data/*.csv
!compose/data/*.py
!compose/data/README.md
!compose/data/archive/
!compose/data/qdrant_storage/
!compose/data/queues/
compose/data/queues/brave_history/*.tmp.sqlite
compose/data/queues/brave_history/brave_history.*.sqlite
!compose/data/queues/brave_history/.sync_state
!compose/data/queues/brave_history/channel_cache.db
compose/data/qdrant.*/
compose/data/qdrant.tmp/
compose/data/queues/processing/
compose/data/n8n/
compose/data/logs/
*.db
*.sqlite
!compose/data/qdrant_storage/**/*.sqlite
!compose/data/queues/brave_history/brave_history.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal
*.journal

# Local helper used during history rewrites; keep out of git
.git-filter-repo.py

# IDE
.vscode/
.idea/
*.swp
*.swo

# Session context management (multi-instance work tracking)
.session/
*.log*.sqlite-shm
*.sqlite-wal
