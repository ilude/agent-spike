apiVersion: 1
providers:
  - name: 'agent-spike'
    orgId: 1
    folder: 'Agent Spike'
    folderUid: 'agent-spike'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
